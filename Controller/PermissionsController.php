<?php

//App::uses('AppController','Controller');
class permissionsController extends BootstrapMenuAppController{
    public function check(){
       //debug('hello');
      // die();
       //debug($this->Acl->check(array('User' => array(AuthComponent::user('id'))), $link["controller"],$link["action"]));
    }
}